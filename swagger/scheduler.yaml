paths:
  /scheduler/publicDataTest:
    post:
      tags:
        - scheduler
      summary: 공공데이터 포털 데이터 가져오기
      description: 공공데이터 포털 데이터 가져오기
      operationId: publicDataTest
      requestBody:
        description: 공지사항 내용을 모두 불러온다.
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
      responses:
        '200':
          $ref: '#/components/responses/200'
        '400':
          $ref: '#/components/responses/400'
        '404':
          $ref: '#/components/responses/404'
        '500':
          $ref: '#/components/responses/500'
  /scheduler/getCityTypeFromPublicData:
    post:
      tags:
        - scheduler
      summary: 공공데이터 포털 데이터 가져오기
      description: 공공데이터 포털 데이터 가져오기
      operationId: getCityTypeFromPublicData
      requestBody:
        description: 공지사항 내용을 모두 불러온다.
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
      responses:
        '200':
          $ref: '#/components/responses/200'
        '400':
          $ref: '#/components/responses/400'
        '404':
          $ref: '#/components/responses/404'
        '500':
          $ref: '#/components/responses/500'
